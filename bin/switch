#!/usr/bin/env bash

mapfile -t wins < <(wmctrl -l | awk '{print $4" "$1}')
sel=$(printf "%s\n" "${wins[@]}" | awk '{print $1}' | dmenu -l 10)
if [ -n "$sel" ]; then
    win=$(printf "%s\n" "${wins[@]}" | grep "^$sel" | awk '{print $2}')
	bspc node -f "$win"
fi
