#!/usr/bin/env bash

if [[ $1 == ..* ]]; then
    exit 0
fi

type=$(file -i "$1" | awk '{print $2}')

if [[ "$type" == *directory* ]]; then
    tree "${@}"
elif [[ "$type" == *text* ]]; then
    batcat "${@}"
fi
