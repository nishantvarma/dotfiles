#!/usr/bin/env rc

file=`{file $1}
mime=`{file -m $1}

echo $file

if (~ $file *executable*) $1
if (~ $mime *text*) vis $*
if (~ $1 *.djvu) spawn zathura $*
if (~ $1 *.jpeg) spawn page $*
if (~ $1 *.mp3) mocp -l $*
if (~ $1 *.mp4) spawn mpv $*
if (~ $1 *.pdf) spawn zathura $*
