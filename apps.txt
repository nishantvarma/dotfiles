A minimal Linux desktop setup.

# Applications
# ============

    2bwm (wm)
    bindfs
    bmpanel (tray)
    clang-format (code)
    cloc (code)
    djview4
    docker
    fd (grep)
    ffmpeg
    flameshot (screenshot)
    ghc (lang)
    git
      git-cola
      git-gui
      gitk
    gnu-netcat
    gvfs-mtp
    highlight (code)
    i3 (wm)
    jwm (wm)
    light
    llpp (pdf)
    mat2 (cleaner)
    meld (diff)
    mkvtoolnix-gui (video)
    mocp (music)
    most
    mpv (video)
    mupdf (pdf)
    notify-osd
    plan9port
      rc (shell)
      rio (wm)
    python (lang)
        black
    racket (lang)
    ripgrep (grep)
    rxvt-unicode
      gtk2
      gtk2-perl
    scid (chess)
    screenkey (screenshot)
    shellcheck
    slock
    socat
    sqlformat
    strace
    ttf-dejavu
    ttf-liberation
    ttf-roboto
    unzip
    vimb (browser)
    vscode
      Docker
      GitLens
      Jupyter
      Jupyter Cell Tags
      Jupyter Keymap
      Jupyter Notebook Renderers
      Jupyter Slideshow
      Markdown All in One
      Material Theme
      Pylance
      Python
      Rainbow CSV
      Remote Development
      Shellcheck
    weechat (irc)
    wget
    xclip
    xorg-server-xephyr
    xorg-xhost
    xsel
    xseticon
    yt-dlp (youtube)
    zip

# Research
# ========

    Kivy
    lite (editor)
    observable
    TinyCC

# Settings
# ========

    Monospace Fonts: Consolas, Dejavu Sans Mono
    Sans Fonts: Noto Sans, Roboto Sans
