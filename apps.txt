A minimal Linux desktop setup.

# Applications
# ============

  2bwm (wm)
  bat (cat)
  bindfs
  bmpanel (tray)
  briss (pdf)
  clang-format (formatter)
  cloc (linter)
  cudatext (editor)
  cuis (smalltalk)
  djview4
  docker
  fd (grep)
  ffmpeg
  freeplane (mind map)
  ghc (lang)
  git
    git-cola
    gitui
  gnu-netcat
  gvfs-mtp
  lapce (editor)
  leo (outliner)
  lem (editor)
  lite-xl (editor)
  llpp (pdf)
  mat2 (cleaner)
  meld (diff)
  mit-scheme (scheme)
  mkvtoolnix-gui (video)
  mocp (audio)
  most
  mpv (video)
  plan9port
    rc (shell)
    rio (wm)
  python (lang)
    black (formatter)
  qemacs (editor)
  racket (lang)
  recoll (grep)
  ripgrep (grep)
  rxvt-unicode
    gtk2
    gtk2-perl
  scid (chess)
  sd (sed)
  shellcheck
  slock
  socat
  sqlformat
  strace
  sysstat
  ttf-dejavu
  ttf-liberation
  ttf-roboto
  unzip
  vimb (browser)
  vscode
    Codeium
    Docker
    Excel Viewer
    Git Graph
    GitLens
    Jupyter
    Jupyter Cell Tags
    Jupyter Keymap
    Jupyter Notebook Renderers
    Jupyter Slideshow
    Leo Editor Integration with Visual Studio Code
    Markdown All in One
    Material Theme
    Pylance
    Python
    Rainbow CSV
    Remote Development
    Shellcheck
    VS Code Speech
  wget
  xclip
  xorg-server-xephyr
  xorg-xhost
  xournalpp (note)
  xsel
  xseticon
  yt-dlp (youtube)
  zed (editor)
  zellij (multiplexer)
  zip

# Settings
# ========

  Monospace Fonts: Consolas, Dejavu Sans Mono
  Sans Fonts: Noto Sans, Roboto Sans
